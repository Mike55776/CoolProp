# jitpack.yml
jdk:
  - openjdk11

before_install:
  - apt-get -qq update && apt-get install -y cmake make gcc g++

# Wir überspringen den Root-Build, und bauen nur das Java-Modul:
install:
  - mvn -B clean install -pl wrappers/Java -DskipTests
